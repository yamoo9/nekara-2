import { describe, test, expect } from './utils/tests.js';

describe('초등 수학', () => {
  test('10 * 20 - 8 = 192', () => {
    expect(10 * 20 - 8).toBe(192);
  });
  test('1 + 1 = 11', () => {
    expect(1 + 1).toBe(11);
  });
})